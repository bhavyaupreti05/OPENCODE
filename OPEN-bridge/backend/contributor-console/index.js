const contributorConsoleRoutes = require('./routes');
const contributorConsoleController = require('./controller');

module.exports = {
  routes: contributorConsoleRoutes,
  controller: contributorConsoleController
};